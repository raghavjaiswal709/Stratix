{
  "name": "stratix-ws-server",
  "version": "1.0.0",
  "private": true,
  "description": "Stratix live-price WebSocket fan-out server (Binance + Finnhub + gold-api).",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  }
}
